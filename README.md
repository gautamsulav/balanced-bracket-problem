# Balanced Bracket Problem

A program to examine whether the pairs and the orders of “{“, “}”, “(“, “)”, “[“, “]” are correct in a given string.

The php file contains a `class` for stack data structure implementation,
a `function` to check balanced bracket
and some sample inputs and outputs.
